# PPL 訓練記錄 App — 專案脈絡

線上網址：https://funraise-hong.github.io/PPL/ （GitHub Pages，`main` branch 根目錄，push 即部署）

## 核心任務

Hong 有一個 90 天身體改變目標：每週工作日中午練 3 次、每次實際運動最多 30 分鐘、只做器械、課表是 PPL（Push / Pull / Legs），一天一個分項。

App 的唯一核心任務：**在健身房現場，30 秒內看到「上次這個動作推多少」，然後填下今天的數字。** 這是支撐 Progressive Overload（漸進超負荷）的工具；任何不能讓「看上次數字」或「填今天數字」更快的功能，大概都不該做。

## 領域詞彙

| 詞 | 意義 |
|---|---|
| 分項 / day_key | A=推 🔥（`#FF8A3D`）、B=拉 💧（`#5C9EFF`）、C=腿 🌿（`#82D94F`），整頁單一強調色隨分項切換；emoji 是保留的御三家彩蛋 |
| 一列一組 | `workout_logs` 一列 = 某人某天某動作的某一組（見 ADR-0002） |
| 上次最重那組 | 每個動作卡片顯示的比較基準，輸入時即時算 `+2.5kg / 持平 / -X kg` 差異標籤——**整個 App 的核心互動** |
| RIR | Reps in Reserve。schema 有欄位、UI 刻意不做（多一欄多一分填寫負擔） |
| 自訂動作 | `user_settings.days` jsonb：per-user 隱藏預設動作 / 新增自訂動作，跨裝置同步 |

## UI 慣例（沿用，不要隨意改掉）

Hong 明確反映過「看名字不知道是哪台機器，廠牌不同名稱也不同」，因此每個預設動作都有：

1. **SVG 線稿示意圖**（`ICONS` 物件）：灰 `#5C6675`=機器、淺灰 `#98A2B0`=人、`var(--day)`=會動的部位與出力方向箭頭，viewBox 統一 `0 0 130 100`。**示意圖會動**（2026-08-14 起）：包在 `mv(dx,dy,…)` 裡的機器部件沿該圖箭頭方向來回平移、箭頭同節奏反覆描繪（共用 2s loop，CSS `machmove`/`machdraw`）；每張圖的方向向量要跟箭頭一致，新增動作圖時記得補 `mv()`；`prefers-reduced-motion` 時全部靜止、箭頭恢復實線
2. **一句「怎麼認」**（`ex.id`）：格式固定為「坐的方向 → 墊子位置 → 出力方向」，這三件事不因廠牌改變
3. **替代動作**（`ex.alt` + `ex.why`）：`why` 解釋動作模式為什麼可互換

容易搞混、特別標註過的（重構時不要弄丟）：蝴蝶機/反向蝴蝶機常是同一台，差在面向哪邊坐；髖外展旁常有外型幾乎一樣的髖內收，墊子在大腿內側。

其他慣例：次數欄預設帶 10、沒填重量的組不算數；深色底、大輸入框（健身房單手操作手機）；填完重量自動起跳 70 秒組間休息倒數（右下角分項色藥丸，mm:ss）。

視覺風格（2026-08-14「清楚・目標感」重設計，取代 08-12 熱血版）：「健身 App 標準式」正統路線，工藝對標 Apple Fitness × Hevy——近黑中性底（`#0A0B0D`）、單一分項強調色、mono tabular 數字、hairline 分隔、中性黑影，設計系統記錄在 `DESIGN.md`。開場即「今天的目標」清單（目標 = 上次最重 +2.5，達標打勾變暗）；分項段控制器在最上方，開啟時依星期自動選分項（一/三/五＝推/拉/腿，其他天選下一個訓練日），左右滑動可切換；點目標列跳到動作卡並聚焦重量欄；40px 目標大數字＋即時差異 pill（↑ +2.5）。御三家梗降為彩蛋：分項 emoji 🔥💧🌿、「進化了！」「體力全滿，上！」微文案、啞鈴 favicon（火/水/草配重片）。`#demo` hash 開離線預覽模式（假資料、擋寫入），設計迭代與截圖用。手機優先；任何寬度都維持一列一卡（明確不要多欄並排），寬螢幕只是容器加寬置中。

## 運維注意

- **RLS 是唯一的安全防線**。anon/publishable key 設計上就是公開的；RLS 沒開 = 資料全裸
- 2026/5/30 後建立的 Supabase 專案，**每張新表都要補 grants** 給 `authenticated`，否則「表存在但 API 讀不到」（`supabase-setup.sql` 已含）
- Supabase 免費專案**七天無請求會休眠**，需到 Dashboard 手動喚醒。偶爾才用的朋友可能中招
- Supabase 內建寄信**每小時上限 2 封**，多裝置登入用 `make link <email>` 免寄信產生連結（需 service_role key，只能在本機終端機用）
- 本機測試：`make serve` 開 http://localhost:8642 ；該網址已加入 Supabase Redirect URLs
- **Repo 公開 ≠ 只有網站公開**：整個 repo（含此文件）都看得到，不要放任何 secret
- 離線容錯：儲存失敗且離線時暫存 localStorage `ppl_pending`，恢復連線自動補送

## 請不要做的事

- 不要為「未來可能很多使用者」先做複雜架構——實際使用者 1 人，免費額度到 5 萬 MAU
- 不要把 RLS 拿掉或改成前端過濾
- 不要在前端放任何 secret key（publishable key 除外）
- 不要簡化掉器械圖和「怎麼認」說明——那是明確要求的功能，不是裝飾
- 不要做 Notion 整合（2026-08-12 已整個取消，含單向匯出）
- 不要投入 Claude 對話沙箱（artifact）版本——持久化已證實走不通，部署成真網頁是正解
